# Test Report: v0.4.0 — Operational Improvements & Admin Tooling

## Summary

| Metric | Value |
|---|---|
| Test suites | 7 (6 pre-existing + 1 new) |
| Tests total | 402 (278 pre-existing + 124 new) |
| Passing | 402 |
| Failing | 0 |
| New test file | `tests/v0.4.0-features.test.ts` |

## New Test Coverage (124 tests)

### Feature 1 — Graceful Shutdown (23 tests)

**expireElevations.ts (5 tests)**
- `ScheduledTask` imported from node-cron
- Return type is `ScheduledTask` (not `void`)
- Cron task captured in named variable before return
- `return task` present at end of function
- Old `void` signature removed

**index.ts (18 tests)**
- `ScheduledTask` imported
- `shutdown()` async function defined
- Signal string parameter present
- `ScheduledTask | null` union type parameter
- `isShuttingDown` boolean guard (double-signal protection)
- SIGTERM and SIGINT handlers registered
- `task.stop()` called with null-guard
- `db.$disconnect()` awaited
- `client.destroy()` called
- `process.exit(0)` on success, `process.exit(1)` on error
- `[Shutdown]` prefix in all log statements
- Task stored and passed to both signal handlers
- Handlers registered after `startExpiryJob` call
- No deprecated `ephemeral: true`

### Feature 2 — Rate Limiting on /elevate (21 tests)

**Structural checks (14 tests)**
- `RATE_LIMIT_WINDOW_MS = 60_000` constant present
- `RATE_LIMIT_MAX_ATTEMPTS = 3` constant present
- Module-level `elevateCooldowns` Map defined
- `isRateLimited()` function defined
- Rate-limit check invoked inside `execute()`
- Map key includes both `guildId` and `discordUserId` (multi-guild isolation)
- Check placed after `deferReply` but before first DB call
- Rejection reply present before DB access
- No audit log entry for rate-limited requests
- User-facing message does not reveal window/threshold numbers

**Pure logic unit tests (7 tests)**
- Allows exactly 3 attempts within 60s
- Blocks the 4th attempt within the window
- Allows a new attempt after the window has fully slid
- Different guild:user key pairs tracked independently
- Same user in two guilds tracked independently
- Stale entries pruned on next access
- Entry at window boundary still counts toward limit

### Feature 3 — Bulk Eligibility Assignment (21 tests)

- `role1` (required), `role2`, `role3` (optional) defined on builder
- Old bare `role` option removed
- `processRole()` helper function exists and accepts `botMember: GuildMember`
- `RoleOutcome` union type covers all three statuses
- `findUnique` precedes `upsert` (idempotency detection)
- Audit log only written for genuinely new assignments
- Admin role guard and hierarchy check inside `processRole()`
- Guild/botMember fetched once before role loop
- Roles deduplicated by ID via `Map`
- Null options filtered before deduplication
- Admin gate checked immediately after `deferReply`
- Reply text distinguishes all three outcome types
- `isWatchtowerAdmin: true` in audit metadata

### Feature 4 — /watchtower-list Completeness (19 tests)

- `db.activeElevation.findMany` query present
- Ordered by `expiresAt: "asc"` (soonest-to-expire first)
- Scoped by `guildId` via `elevationWhere` variable
- `pimUser` relation included
- `MAX_ASSIGNMENT_FIELDS = 20` and `MAX_ELEVATION_FIELDS = 5`
- Combined field budget ≤ 25 (Discord limit)
- Relative Discord timestamp (`:R>`) in elevation entries
- "None currently active." shown when no elevations
- "No PIM access records found." empty state
- Separate embed titles for filtered vs. unfiltered view
- "Active Elevations" section label present
- Footer truncation note present

### Feature 5 — /watchtower-audit Command (37 tests)

**Structural (27 tests)**
- Command named `watchtower-audit`
- `user` and `recent` subcommands defined
- Required user option on `user` subcommand
- Integer limit option with `setMinValue(1)` / `setMaxValue(25)` on both
- No `setDefaultMemberPermissions` (admin gate is sole control)
- `isWatchtowerAdmin` checked after `deferReply`
- `getOrCreateGuildConfig` used for guild config
- `eventTypeEmoji` imported from `lib/audit`
- `user` subcommand queries by both `guildId` and `discordUserId`
- `recent` subcommand queries by `guildId` only
- Both queries ordered by `createdAt` descending
- Both queries use `take` parameter
- `DEFAULT_LIMIT = 10` constant defined
- `buildAuditEmbed()` helper defined
- `EMBED_CHAR_BUDGET` constant present and ≤ 6000
- Truncation footer text included
- `eventTypeEmoji` used inside `buildAuditEmbed`
- `roleName` rendered conditionally
- `metadata` field NOT rendered (security)
- Zero-results plain text reply
- `MessageFlags.Ephemeral` used
- Unknown subcommand fallback handled

**Embed logic unit tests (3 tests)**
- All entries shown when well within budget
- Entries truncated when budget exceeded
- Zero entries shown if first entry alone exceeds budget

**audit.ts export (2 tests)**
- `eventTypeEmoji` is a named export
- Not a default export

### Cross-cutting (3 tests)
- `help.ts` contains `/watchtower-audit` with both subcommand descriptions
- `/watchtower-assign` description mentions "3 roles at once"
- `/watchtower-list` description mentions "elevations"

## Pre-existing Test Suite (278 tests — all still passing)

| Suite | Tests | Status |
|---|---|---|
| `admin-guard.test.ts` | 41 | PASS |
| `duration.test.ts` | 28 | PASS |
| `expiry-notifications.test.ts` | 135 | PASS |
| `help-command.test.ts` | 16 | PASS |
| `password-reset.test.ts` | 28 | PASS |
| `permissions.test.ts` | 30 | PASS |

One pre-existing test assertion was updated during implementation (admin-guard test for the assign command's admin-role guard text, which changed from an inline "Warning:" reply to a structured `skipped` outcome with a descriptive reason string).
